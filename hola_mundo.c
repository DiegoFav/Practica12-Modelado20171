#include <stdio.h>			//incluimos la libreria stdio para poder imprimir en pantalla

int main() {				//declaramos y definimos nuestra funcion main(la principal)
	printf("¡Hola, mundo!");	//usamos printf de stdio.h donde imprimimos la cadena "¡Hola, mundo!"
	return 0;					//regresamos un entero (0) que nos pide la firma de la funcion que hagamos
}
